# Requirements for GitHub Actions CI (inbox monitor only)
# Much lighter than the full requirements.txt — skips torch, optuna, matplotlib, etc.
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
xgboost>=2.0.0
openpyxl>=3.1.0
