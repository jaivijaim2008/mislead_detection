services:
  - type: web
    name: missed-lead-detector
    runtime: docker
    plan: free
    dockerfilePath: Dockerfile
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
